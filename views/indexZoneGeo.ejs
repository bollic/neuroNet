 <!-- views/indexZoneGeo.ejs -->
<%- include('layout/headerZoneGeo') %>
 

<style>
  html, body { height: 100%; }
  #map {   height: calc(100vh - 100px);  /* 60px = altezza del footer */
  width: 100%; }
  .leaflet-container {
  z-index: 0 !important;
}
#drawer-toggle-btn {
  position: fixed;
  bottom: 6rem; /* circa 96px */
  left: 1rem;
  z-index: 3000;
}
</style>
<style>
.drawer-side {
  z-index: 50 !important;
}
</style>
<style>
  .fab {
  position: absolute;
  bottom: 20px;
  right: 20px;
  width: 50px;
  height: 50px;
  border-radius: 50%;
  font-size: 24px;
  background: #2c7be5;
  color: white;
  border: none;
  cursor: pointer;
}

.map-wrapper {
  position: relative;
  height: 100%;
}



.map-overlay {
  position: absolute;
  top: 20px;
  right: 20px;
  width: 350px;
  z-index: 1000;
}

.hidden {
  display: none;
}

.overlay-card {
  background: white;
  padding: 20px;
  border-radius: 12px;
  box-shadow: 0 8px 30px rgba(0,0,0,0.15);
}

.close-btn {
  position: absolute;
  top: 10px;
  right: 15px;
  background: none;
  border: none;
  font-size: 18px;
  cursor: pointer;
}
</style>
<style>
/* Forza la visualizzazione delle emoji nei marker personalizzati */
.leaflet-div-icon {
  background: none !important;
  border: none !important;
}
.leaflet-div-icon div {
  font-family: "Apple Color Emoji", "Segoe UI Emoji", "Noto Color Emoji", sans-serif;
  text-align: center;
}
</style>
<style>
.highlight-row {
    background-color: #ffe9a8 !important;
    transition: background-color 0.3s ease
}
</style>
<style>
  #group-feed {
    max-height: none !important;   /* niente limite altezza */
    overflow-y: visible !important; /* elimina scrollbar verticale */
  }
</style>
<style>
  #profile-section {
    min-height: calc(100vh - 60px); /* spazio per il footer */
    overflow-y: auto;
  }
  footer {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    z-index: 50;
  }
</style>
<style>
#main-table {
  table-layout: fixed; /* forza larghezza colonne */
  width: 100%;
}
/* Celle e intestazioni */
#main-table th, 
#main-table td {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  text-align: center;
  vertical-align: middle;
  font-size: 0.85rem;
  padding: 6px;
}
/* Larghezze colonne */
#main-table td:nth-child(1),
#main-table th:nth-child(1) { width: 40px; }

#main-table td:nth-child(2),
#main-table th:nth-child(2) { width: 60px; }

#main-table td:nth-child(3),
#main-table th:nth-child(3) { 
  width: 40px; 
  text-align: center; 
}
/* Bottone Delete più compatto */
#main-table .btn-error {
  min-width: 24px;
  height: 24px;
  padding: 0;
}
</style>

   <!-- 1: inizio flex -->
<div class="flex flex-row h-screen">
<!-- 2: drawer -->
    <div class="drawer drawer-start drawer-mobile md:drawer-open">
      <input id="my-drawer" type="checkbox" class="drawer-toggle" />
      <!-- Contenuto principale (mappa) -->
      <div class="drawer-content relative">
    <!-- Pulsante toggle sempre visibile -->
      <!-- 👋 ONBOARDING -->
<% if (showWelcomeAddPoint) { %>
  <%- include('partials/onboardingAddAction', {
    description: "Vous pouvez maintenant ajouter des signalements sur la carte.",
    actionUrl: "/addPoint",
    actionLabel: "Ajouter un signalement"
  }) %>
<% } %>


          <!-- unica mappa -->
     <div id="content-sections" class="relative w-full h-full">
  <!-- 🗺️ MAPPA -->
  <div id="map-section" class="p-4 h-[calc(100vh-64px)]">
    <div id="map" class="w-full h-full rounded-lg shadow-md"></div>
    
  <!-- Overlay Form -->
  <div id="map-overlay" class="map-overlay hidden">
    <div class="overlay-card">
      <button type="button" class="close-btn" onclick="closeOverlay()">✕</button>

      <h3 id="overlay-title">Nouveau Signalement</h3>
       <!-- QUI DENTRO METTI IL FORM ESISTENTE -->
    <%- include('partials/addPointForm.ejs') %>
   
    </div>
  </div>
    <!-- 🔴 Pulsante Add Point centrato sopra il footer -->
    <button
      id="open-add-point"
      class="fixed bottom-20 left-1/2 -translate-x-1/2 z-10 bg-white border border-gray-300 rounded-full p-3 shadow-lg hover:scale-110 transition-transform">
      <i class="fas fa-plus text-gray-700 text-xl"></i>
    </button>

  </div>
<!-- 🕸️ Pulsante toggle drawer (resta sopra tutto) -->
<a id="drawer-toggle-btn"
   class="fixed bottom-24 left-4 z-[3000]">
  <label for="my-drawer"
         class="bg-white border border-gray-300 rounded-full p-2 cursor-pointer text-3xl hover:scale-110 transition-transform shadow-lg">
    🕸️
  </label>
</a>
 <!-- 📍 Pulsante geolocalizzazione -->
<button id="use-my-location"
        class="fixed bottom-24 right-4 z-[3000] bg-white border border-gray-300 rounded-full p-3 shadow-lg hover:scale-110 transition-transform">
  <i class="fas fa-crosshairs text-gray-700 text-xl"></i>
</button>

<!-- 👥 Toggle punti gruppo -->
 <div class="fixed bottom-40 right-4 z-[3000] flex items-center gap-2 text-xs bg-white/90 backdrop-blur px-3 py-2 rounded-full shadow-lg">


  <span>👥 Groupe</span>
  <input type="checkbox" id="toggleGroupPoints" class="toggle toggle-primary toggle-sm" />
</div>
      <!-- 👤 PROFILO (inizialmente nascosto) -->
   <%# include('partials/profileSection', { user: user, referenteOffice: referenteOffice }) %>
</div>
      </div>   
        <!-- Sidebar- a sinistra -->
        <div class="drawer-side">
        <label for="my-drawer" class="drawer-overlay"></label>
         <aside class="w-80 bg-base-200 p-4 overflow-y-auto pb-20"
       style="height: calc(100vh - 60px);">
       <div id="point-card"
     style="position:fixed; left:-9999px; top:0; width:350px; background:white; padding:20px; border-radius:12px;">
  <h3 id="share-title"></h3>
  <img id="share-image" src="" style="width:100%;border-radius:8px;margin:10px 0;">
  <p id="share-description"></p>
  <p id="share-date" style="font-size:12px;color:gray;"></p>
  <p style="font-size:12px;color:gray;">via terria.app</p>
</div>
<!-- 👤 Info utente -->
   <%- include('partials/sidebarProfile.ejs') %>
<!-- Aggiungi switch “Mostra punti del gruppo” -->
   <%- include('partials/sidebarToggleGroupPoints.ejs') %>

    <hr class="my-2">
    <!-- 🔹 Tabella dei punti con DataTables -->
    <%- include('partials/tablePoints') %>
    <!-- Feed del gruppo -->

          <button id="closeSidebar" class="close-btn d-md-none" data-bs-toggle="collapse" data-bs-target="#sidebar">&times;</button><br>   
        </aside>        
    <script>
    // tolto async function loadGroupFeed 
    </script>
    </div>
    <!-- Fine .container-fluid -->

    <!-- ✨ Toast di benvenuto dinamico solo per utenti 'field' -->

    <!-- ✨ Fine toast -->
    <!-- 💫 FLASH MESSAGE SESSIONE (onboarding field) -->

    <script>
      window.points = <%- JSON.stringify(points || []) %>;
      window.currentUserId = "<%= user._id %>";
      window.CATEGORIES = <%- JSON.stringify(categories || []) %>;
      console.log("🧪 categorie dell'utente field:", window.CATEGORIES);
      console.log("🧪 categorie dell'utente field:", <%- JSON.stringify(categories || []) %>);
      console.log("🔎 Points lato client:", <%- JSON.stringify(points || []) %>);
    </script>
    </div> <!-- 🔒 chiusura drawer -->
</div> <!-- 🔒 chiusura flex-row -->
<%- include('layout/footer') %>

<script>
  const openProfileBtn = document.getElementById('open-profile');
  const goPlanBtn = document.getElementById('go-plan');
  const mapSection = document.getElementById('map-section');
  const profileSection = document.getElementById('profile-section');

  function openProfile() {
    mapSection.classList.add('hidden');
    profileSection.classList.remove('hidden');
  }

  function closeProfile() {
    profileSection.classList.add('hidden');
    mapSection.classList.remove('hidden');
    if (window.map && typeof window.map.invalidateSize === 'function') {
      setTimeout(() => window.map.invalidateSize(), 300);
    }
  }

  if (openProfileBtn) openProfileBtn.addEventListener('click', openProfile);
  if (goPlanBtn) goPlanBtn.addEventListener('click', closeProfile);

  // ESC per chiudere il profilo
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeProfile();
  });
</script>

<script type="module" src="/js/dataAuthorGeo.js" defer></script>
<script type="module" src="/js/formMapIntegration.js"></script>